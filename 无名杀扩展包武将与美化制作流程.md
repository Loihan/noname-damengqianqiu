# 无名杀扩展包武将与美化制作流程

> 本流程文档综合自《无名杀代码教程》（扩展包目录内自写教程）与对无名杀引擎源码（`noname/` 目录：扩展加载、事件系统、`chooseButton`/对话框、`ui`/`layout` 样式体系）的研读实践，并以本扩展（大梦千秋）曹髦【缚渊】的主题选择框与卡牌特效为实战案例。
>
> 阅读前建议先通读《无名杀代码教程》第一章至第四章，本流程文档侧重「从零做出一个带美化的扩展包」的完整路径，并补充了教程未细讲的：现代 ESM 扩展骨架、自定义对话框 UI、样式与美术资源接入、调试方法。

---

## 目录

- [第〇章 扩展包是什么](#第〇章-扩展包是什么)
- [第一章 扩展包骨架：extension.js](#第一章-扩展包骨架extensionjs)
- [第二章 武将制作](#第二章-武将制作)
- [第三章 技能制作（核心）](#第三章-技能制作核心)
- [第四章 选择交互：chooseButton 与自定义对话框](#第四章-选择交互choosebutton-与自定义对话框)
- [第五章 美化与特效制作](#第五章-美化与特效制作)
- [第六章 音频接入](#第六章-音频接入)
- [第七章 调试、验证与发布](#第七章-调试验证与发布)
- [附录一 常用词汇表（摘自《无名杀代码教程》）](#附录一-常用词汇表摘自无名杀代码教程)
- [附录二 大梦千秋曹髦文件清单](#附录二-大梦千秋曹髦文件清单)

---

## 第〇章 扩展包是什么

无名杀的扩展包（extension）是一个**目录**（或 zip 包），放在游戏根目录的 `extension/` 下，例如：

```
extension/
└── 大梦千秋/
    ├── extension.js          # 扩展主入口（必填）
    ├── info.json             # 扩展信息（名称/作者/版本）
    ├── CHANGELOG.md          # 更新日志（可选）
    ├── README.md             # 说明（可选）
    ├── character/            # 武将/技能模块（本包的组织方式）
    │   ├── sgz_caomao.js
    │   └── ...
    ├── image/                # 图片素材
    │   ├── sgz_caomao.jpg
    │   └── sgz_caomao_dialog_bg.svg
    └── audio/                # 音频素材
        └── sgz_caomao/
            ├── sgz_fuyuan1.mp3
            └── ...
```

- 启动时无名杀会加载每个已启用扩展目录下的 `extension.js`。
- 扩展内文件的引用路径有两种写法：
  - **相对游戏根目录**：`extension/大梦千秋/image/sgz_caomao.jpg`（在 CSS 背景、`img` 字段、`setBackgroundImage` 等场景常用）
  - **`ext:` 前缀**：`ext:大梦千秋/audio/sgz_caomao/die.mp3`（在 `dieAudios`、技能 `audio` 等引擎解析音频/图片的字段中常用）

---

## 第一章 扩展包骨架：extension.js

### 1.1 两种写法

**旧式写法**（《无名杀代码教程》第四章 01 节）：用 `game.import('extension', {...})` 在全局上注册。较老的扩展多用此方式。

**现代 ESM 写法**（本包采用）：`extension.js` 是一个 ES Module，`export default` 返回扩展配置对象。无名杀（`noname/init/loading.js` 的 `loadExtension`）会导入它并调用导出的函数。

### 1.2 完整骨架（本包 `extension.js` 精简版）

```js
// 1. 引入引擎全局（非必选，但用到 game/ui/get 等时引入便于阅读与类型提示）
import { lib, game, ui, get, ai, _status } from "../../noname.js";

// 2. 引入各武将模块（每个模块是一个 character/skills 数据文件）
import sgzCaomao from './character/sgz_caomao.js';

export const type = "extension"; // 必须

export default function () {
    return {
        name: "大梦千秋",              // 扩展名（需与文件夹名一致）
        content: function (config, pack) {
            // 游戏加载时执行，可修改游戏本体
            // 常见用法：
            lib.arenaReady.push(function () {
                // 界面创建完毕后执行（设置强度、威胁度等）
            });
            // 给“千幻聆音”类扩展注册台词包等
        },
        precontent: function () {
            // 在游戏内容加载前执行，常用于全局劫持（详见第五章 5.5）
        },
        config: { /* 扩展设置项，会显示在扩展菜单 */ },
        help: {},                     // 帮助面板内容
        package: {
            character: {
                character: Object.assign({}, ...武将模块.character),
                translate: Object.assign({}, ...武将模块.characterTranslate),
                characterSort: { /* 武将包内部分类 */ },
            },
            skill: {
                skill: Object.assign({}, ...武将模块.skills),
                translate: Object.assign({}, ...武将模块.skillTranslate),
            },
            intro: "扩展包简介",
            author: "作者",
            version: "7.1",
        },
        files: { character: [], card: [], skill: [], audio: [] },
    };
}
```

要点：
- `package.character.character` 把各武将模块的 `character` 字段合并，即注册武将。
- `package.skill.skill` 合并注册技能。
- 若走 `game.import` 旧式写法，武将加进 `lib.character`、技能加进 `lib.skill`、翻译加进 `lib.translate`（见教程第四章 01~03 节）。

### 1.3 常用生命周期钩子

| 钩子 | 时机 | 用途 |
| --- | --- | --- |
| `content` | 扩展载入时（开局前） | 注册武将/技能/设置 |
| `precontent` | 内容加载前 | 劫持引擎方法（音频、皮肤等） |
| `lib.arenaReady.push(fn)` | 界面创建完毕后 | 设置强度、威胁度、前言等 |
| `lib.packageReady.push(fn)` | 扩展载入完毕、界面创建前 | 修改现有武将包（教程 3.1.2） |

---

## 第二章 武将制作

### 2.1 武将数据对象（现代写法，`character/sgz_caomao.js`）

```js
export default {
    character: {
        sgz_caomao: {            // 武将代码名（唯一）
            sex: "male",         // 性别：male / female / unknown / neuter
            group: "shen",       // 势力：wei shu wu qun shen 或自定义
            hp: 3,               // 体力
            maxHp: 4,            // 体力上限（可省略，默认=hp）
            hujia: 1,            // 初始护甲
            skills: ["sgz_fuyuan", "sgz_qingtao", "sgz_caomao_qianlong_ui"],
            img: "extension/大梦千秋/image/sgz_caomao.jpg", // 头像（立绘）
            dieAudios: ["ext:大梦千秋/audio/sgz_caomao/die.mp3"], // 阵亡语音
            names: "曹|髦",       // 双将名（主|副），单将可不写
            groupInGuozhan: "wei",// 国战势力
            4: ["des:曹髦，字彦士……"], // 武将介绍（“des:”前缀，支持 <br>）
        },
    },
    characterName: 'sgz_caomao',              // 供合并逻辑识别的武将名
    characterTranslate: { sgz_caomao: "曹髦" }, // 显示名
    skills: { /* 见第三章 */ },
    skillTranslate: { /* 技能名与技能描述 */ },
    characterTaici: { /* 台词包（千幻聆音可读取） */ },
};
```

**旧式写法对照**（教程 3.1/3.3）：`lib.character.x_caocao = ['male','wei',4,['x_jianxiong'],['db:extension-xxx:x_caocao.jpg','zhu']]`。其中第 5 项数组的标记位：`zhu` 常备主公、`boss`、`bossallowed` 等。便捷写法 `game.addCharacter(...)`。

### 2.2 强度、禁选、珠联璧合

```js
lib.arenaReady.push(function () {
    lib.rank.rarity.legend.add('sgz_caomao');  // 传说品质（现代写法）
    lib.rank.s.push('x_caocao');               // 旧式：S 级强度
});
lib.config.threaten['sgz_caomao'] = 119.6;     // 威胁度（影响 AI 判断）
lib.config.forbidai.push('x_caocao');          // AI 禁选
lib.config.forbiddouble.push('x_caocao');      // 双将禁选
lib.perfectPair.x_caocao = ['dianwei'];        // 珠联璧合
```

### 2.3 武将包分类

```js
characterSort: {
    "大梦千秋": {                 // 扩展名
        "三国志": ['sgz_caomao', 'sgz_simazhao'],
        "山海经": ['shj_baize'],
    }
}
```

分包名翻译：`lib.translate["三国志"] = "<span style='color:#ff3333;'>三国志</span>"`（支持 HTML 着色）。

---

## 第三章 技能制作（核心）

> 本章是对《无名杀代码教程》第一章~第四章的浓缩+本包实践，建议配合原教程食用。

### 3.1 技能四大框架（先判断再动手）

| 框架 | 特点 | 关键字段 |
| --- | --- | --- |
| **主动技** | 某时段内可发动 | `enable`（如 `"phaseUse"`）|
| **触发技** | 特定时机自动触发 | `trigger`（`{ player: "damageEnd" }`）|
| **视为技** | 牌转化 | `viewAs` + `filterCard`/`selectCard` + `position` |
| **mod 技** | 无发动，常驻影响 | `mod`（`cardUsable`/`targetInRange`/`cardEnabled2`…）|

### 3.2 最简单的三种技能（摘自教程 01 节）

```js
// 主动技：出牌阶段发动，摸一张牌
sgz_demo1: {
    enable: "phaseUse",
    content: function () { player.draw(); },
},

// 触发技：你的回合开始时触发
sgz_demo2: {
    trigger: { player: "phaseBegin" },
    content: function () { player.draw(); },
},

// 视为技：将一张手牌当【杀】使用
sgz_demo3: {
    enable: "phaseUse",
    selectCard: 1,
    filterCard: true,   // 任意手牌
    position: "h",
    viewAs: { name: "sha" },
    ai: { order: 5, result: { player: 1 } },
},
```

### 3.3 技能通用字段

- `filter(event, player)`：能否发动的判定（不写默认可发动）
- `filterTarget(card, player, target)`：目标限制
- `selectTarget`：选目标个数
- `content(event, trigger, player)`：发动内容（`trigger` 为触发源事件）
- `audio`：发动语音，如 `"ext:大梦千秋/audio/sgz_caomao:16"`（表示该目录下 1~16 号随机播放）
- `forced`：锁定技
- `limited` + `mark:false`：限定技
- `persevereSkill`：持恒技（本包特色，跨回合持续生效）
- `charlotte`：隐藏技能（不出现在技能栏，如标记辅助技能）
- `skillAnimation` + `animationColor`：发动时的大字动画（"fire"/"thunder"/"water"/"wood"/"earth"）
- `derivation`：衍生技能
- `intro`：标记说明（`{ name, content }`）

### 3.4 content 的分步写法（教程 08 节）

需要等待玩家选择（异步）时用 `"step 0"` / `"step 1"` 分步，每步之间会等待事件完成：

```js
content: function (event, trigger, player) {
    "step 0";
    player.chooseTarget("请选择一名角色");
    "step 1";
    if (result.bool) result.targets[0].draw();
},
```

**更现代的做法**：用 `async content` + `await ...forResult()`（本包曹髦采用），逻辑更直白：

```js
async content(event, trigger, player) {
    const res = await player.chooseTarget("请选择一名角色", (card, player, target) => target != player, true)
        .set("ai", function (target) { return get.attitude(this.player, target) < 0 ? 10 : 0; })
        .forResult();
    if (!res.bool || !res.targets.length) return;
    const target = res.targets[0];
    target.draw();
},
```

### 3.5 事件与 set()（教程第二章）

- 事件函数（`chooseTarget`/`chooseButton`/`chooseCard`…）的**返回值是该事件对象**，可连续 `.set()`。
- `set("属性名", 值)` 为该事件挂自定义属性，可在 `ai` 回调里通过 `_status.event.属性名` 读取。
- `get.player()` 等价于 `_status.event.player`，即当前事件的主人。

### 3.6 判定（教程 10 节）

```js
player.judge(function (card) { return card.color == 'red' ? 1 : -1; });
// 结果存于 result.judgeResult：{ bool, color, number, card, judge }
```

### 3.7 标记技能（教程 13 节，邓艾【急袭】范式）

```js
sgz_demo_mark: {
    trigger: { player: "phaseBegin" },
    content: function () { player.addMark('sgz_demo_mark', 1); },
    marktext: "谋",            // 标记上的字
    intro: { name: "谋划", content: "标记#个" },
    onremove: function (player) { /* 标记移除时 */ },
},
```

- `player.addMark(name, num)` / `removeMark` / `countMark`
- 标记显示名：`skillTranslate` 中 `技能名_marktext` / `技能名_info`
- `player.storage.xxx` 是技能的持久存储（整局有效，教程 09 节）

### 3.8 子技能 subSkill

把辅助逻辑拆成子技能，可分别控制开关：

```js
sgz_fuyuan: {
    group: "sgz_fuyuan_xiaoguo",   // 关联子技能组
    subSkill: {
        xiaoguo: {                 // 完整技能名：sgz_fuyuan_xiaoguo
            trigger: { global: "roundStart", player: "damageEnd" },
            content: function () { /* … */ },
        },
    },
},
// 通过 player.hasSkill('sgz_fuyuan_xiaoguo') / addSkill / removeSkill 控制
```

### 3.9 AI 编写（教程第三章）

```js
ai: {
    order: 5,                  // 出牌阶段使用优先级（越大越先）
    expose: 0.5,               // 暴露度（0~1，影响身份判断）
    threaten: 3,               // 威胁度（影响对手集火倾向）
    result: {
        player: 1,             // 主动技收益评分
        target: function (player, target) { return -get.threaten(target); },
    },
    maixue: true,              // 卖血标签
    maixie_defend: true,       // 卖血防御标签
},
```

- 触发技的发动决策看 `ai.result.player`（>0 才发动）。
- `chooseTarget` 的 AI 写在 `.set("ai", fn)`，`fn(target)` 返回分数，越大越优先。
- `chooseButton` 的 AI 同理，`fn(button)` 按 `button.link` 打分。

### 3.10 mod 技能

```js
mod: {
    cardUsable: function (card, player, num) { return Infinity; }, // 无限次数
    targetInRange: function (card, player) { return true; },       // 无距离限制
    cardEnabled2: function (card, player) { return false; },       // 禁止使用
    ignoredHandcard: function (card, player) { return true; },     // 不计入手牌上限
    cardDiscardable: function (card, player, name) { return false; },
},
```

---

## 第四章 选择交互：chooseButton 与自定义对话框

### 4.1 三种选择事件

| 事件 | 用途 | 结果 |
| --- | --- | --- |
| `chooseTarget` | 选角色 | `result.targets` |
| `chooseControl` | 几个文字选项 | `result.control` |
| `chooseButton` | 自定义按钮/对话框 | `result.links`（各按钮的 `link` 值数组）|

### 4.2 chooseButton 基础用法（引擎自带选项框）

```js
const res = await player.chooseButton([
    "提示文字",
    [choices, "textbutton"],   // choices: [['id1','文字1'], ['id2','文字2']]
]).set("ai", function (button) {
    return button.link == 'id1' ? 10 : 0;   // AI 打分
}).set("forced", true).forResult();
// res.links[0] 即所选 id
```

### 4.3 自绘对话框：把 dialog 对象直接交给 chooseButton

`chooseButton` 接受一个**已构建好的对话框对象**（`get.itemtype(obj) == "dialog"` 时走 `next.dialog = obj` 分支），引擎的 AI 判定（`ai.basic.chooseButton` 遍历 `dialog.buttons`）、点击选择（`ui.click.button`）、确定结算（`ui.click.ok` 取 `get.links(ui.selected.buttons)`）全部照常工作。**这是做主题选择框的正路**——不破坏事件系统、AI、联机、录像回放。

关键机制（引擎源码：`noname/library/element/dialog.js`、`content.js` 的 `chooseButton`）：

1. 创建对话框：`var dlg = ui.create.dialog('hidden')`，内容塞进 `dlg.content`。
2. 把每个选项做成一个 div：设 `el.link = 'opt1'`，加入 `dlg.buttons` 数组，点击事件挂 `ui.click.button`（并 `Object.setPrototypeOf(el, lib.element.Button.prototype)` 以便 `get.links` 等复用）。
3. `player.chooseButton(dlg).set("ai", fn).set("forced", true).forResult()`。

### 4.4 实战：曹髦【缚渊】“帝诏·权能”主题选择框

完整代码见 `character/sgz_caomao.js` 顶部模块，要点如下：

```js
function dmqcBuildFuyuanDialog(player, target, choices) {
    dmqcInjectFuyuanStyle();                       // 注入一次性 <style>
    var dlg = ui.create.dialog('hidden');
    dlg.classList.add('dmqc-fuyuan-dialog');

    // (1) 关键布局内联化 + 整体缩放（不依赖样式表、杜绝引擎全局 div 规则干扰）
    var vw = window.innerWidth || 1280;
    var designW = 860, dlgW = Math.min(designW, vw - 24);
    var scale = Math.min(1, (vw - 24) / designW);
    dlg.style.cssText = 'position:absolute;left:50%;top:50%;width:' + dlgW + 'px;height:auto;...';
    dlg.style.setProperty('--dmqc-width', dlgW + 'px');   // 注意：cssText 之后设置，否则被清空
    dlg.style.setProperty('--dmqc-scale', scale);          // 缩放驱动 transform: scale()

    var wrap = ui.create.div('dmqc-fy', dlg.content);
    wrap.style.cssText = 'position:relative;display:block;box-sizing:border-box;width:' + dlgW + 'px;...';

    // (2) 五个权能卡：单行排列、固定宽度、不换行
    var opts = ui.create.div('dmqc-fy-opts', wrap);
    opts.style.cssText = 'position:relative;display:flex;flex-wrap:nowrap;justify-content:center;gap:12px;';
    choices.forEach((c, i) => {
        var btn = ui.create.div('dmqc-fy-opt', opts);
        btn.style.cssText = 'position:relative;flex:0 0 140px;box-sizing:border-box;width:140px;...';
        btn.link = c[0];
        btn.innerHTML = '<div class="dmqc-fy-opt-seal">放</div>...';
        // 点击权能卡仅“选中/取消选中”（火焰边框高亮），真正生效由“诏令”按钮确认
        btn.addEventListener('click', function () {
            var evt = _status.event;
            if (evt && evt.name == 'chooseButton' && evt.player && evt.player.isMine()
                && !_status.auto && !_status.video) {
                if (selectedBtn === this) { selectedBtn = null; this.classList.remove('selected'); }
                else {
                    if (selectedBtn) selectedBtn.classList.remove('selected');
                    selectedBtn = this;
                    this.classList.add('selected');   // 触发火焰边框动画
                }
                dmqcFyUpdateEdict();                  // 高亮/禁用诏令按钮
            } else {
                ui.click.button.call(this);           // 托管/录像等走引擎默认
            }
        });
        Object.setPrototypeOf(btn, lib.element.Button.prototype);
        dlg.buttons.push(btn);
    });

    // 诏令（确认）按钮：选中权能后高亮，点击后正式生效
    var edictBtn = ui.create.div('dmqc-fy-edict disabled', edictRow);
    edictBtn.addEventListener('click', function () {
        var evt = _status.event;
        if (evt && evt.name == 'chooseButton' && evt.player && evt.player.isMine()
            && !_status.auto && !_status.video) {
            if (!selectedBtn) return;                 // 未选中：抖动提示
            if (ui.confirm) ui.confirm.close();       // 兜底关闭可能存在的确认栏
            evt.result = { bool: true, buttons: [selectedBtn], cards: [], targets: [],
                           confirm: "ok", links: [selectedBtn.link] };
            game.uncheck();                           // 清理选中态/selecting 类/imchoosing（与 ui.click.ok 一致）
            game.resume();
        }
    });
    return dlg;
}
```

要点：
- **“选中”与“生效”分离**：选项点击只切换 `selected` 类（配合 CSS 火焰流动边框动画），`selectedBtn` 记录当前选中卡；**诏令**按钮（独立于 `dlg.buttons`，不参与引擎选择计数）在选中后高亮脉冲，点击时才用 `selectedBtn.link` 直接结算。
- 这样既隐藏了引擎原生“确定/取消”栏（见 4.5），又提供了有角色特色的确认交互。

同时，事件上要加 `noconfirm`（引擎 `game.Check.confirm` 会检查 `_status.event.noconfirm`，为真时**根本不创建**确认栏）：

```js
res_choice = await player.chooseButton(dmqcBuildFuyuanDialog(player, target, choices))
    .set("ai", fuyuanAi).set("forced", true).set("noconfirm", true).forResult();
```

**踩坑记录（重要）**：

1. **无名杀默认布局 css 有全局规则 `div { display:inline-block; position:absolute; transition:all .5s }`**（`layout/default/layout.css`，主菜单常驻加载，对局中依然生效）。自定义 UI 的每个 div 必须显式设 `position` 与 `display`，否则元素会全部绝对定位堆叠在一起。
2. **flex 换行坑**：选项行若 `flex-wrap: wrap`，窗口窄时卡片会换行堆叠。对策：`nowrap` + 固定 `flex-basis` + 整个面板用 `transform: scale()` 等比缩放（`--dmqc-scale`）。
3. **`style.cssText` 会清空同元素上先前 setProperty 的自定义属性**：务必先赋 `cssText`，再 `setProperty('--dmqc-scale', ...)`。
4. **引擎原生“确定/取消”栏（`ui.control`，z-index 5）会浮在自绘对话框（z-index 4）上方**：见上，单选完成时手动 `ui.confirm.close()` + `ui.click.ok()` 自动确定即可（引擎的 `autoConfirm` 钩子只在非鼠标点击场景生效，鼠标点击时 `_status.mousedown` 为真，不会自动确定，必须自己处理）。
5. **联机/录像/托管兼容**：自绘 DOM 无法在远端重建，`chooseButton` 事件也会被序列化。因此仅在 `player.isMine() && !game.online && !_status.auto && !_status.video` 时用自绘对话框，否则回退引擎默认 `["提示", [choices, "textbutton"]]`。

### 4.5 隐藏原生确认/取消按钮的通用做法

- **方案 A（推荐，本包采用）**：双管齐下——
  1. 事件挂 `.set("noconfirm", true)`：引擎 `game.Check.confirm`（`noname/game/check.js`）检查 `_status.event.noconfirm`，为真时**跳过 `ui.create.confirm`**，原生“确定/取消”栏从一开始就不会被创建（这也解释了为什么单纯在点击后 `ui.confirm.close()` 不够：强制选择（`forced`）事件在打开时 `ok` 即为真，确认栏在**点击前**就可能已经出现）；
  2. 用**自绘确认按钮替代**（如缚渊的“诏令”按钮）：选项点击只做选中标记，确认按钮点击时手动写 `evt.result = { bool:true, buttons:[selected], links:[selected.link] }` + `game.uncheck()` + `game.resume()` 直接结算，完全不经过 `ui.click.button → game.check → ui.create.confirm` 流程。
- 方案 B：沿用 `ui.click.button` + 选择完成后 `ui.confirm.close()` + `ui.click.ok()`（仅在点击后同一帧关闭，体验上偶有闪烁，不如方案 A 彻底）。
- 方案 C：临时劫持 `ui.create.confirm` 为空函数（不推荐，影响面大）。
- 注意：引擎自带的 `autoConfirm` 钩子只在**非鼠标点击**场景（`_status.mousedown` 为假，如自动/超时）生效，真实点击时不会自动确定，不能依赖它。

---

## 第五章 美化与特效制作

### 5.1 图片素材

- 武将立绘/头像：放 `image/`，`character.img` 引用（jpg/png）。
- 图片路径两种写法见第〇章。
- 皮肤/换肤：可参考「千幻聆音」扩展，注册 `lib.qhlypkg` 台词/皮肤包（本包 `extension.js` 已示范），或劫持 `HTMLDivElement.prototype.setBackgroundImage` 实现全局换肤。

### 5.2 CSS 注入（两种方式）

**方式一：运行时注入 `<style>`（本包采用，幂等）**

```js
function dmqcInjectFuyuanStyle() {
    if (document.getElementById('dmqc_fuyuan_style')) return;   // 只注入一次
    var style = document.createElement('style');
    style.id = 'dmqc_fuyuan_style';
    style.innerHTML = `/* 你的 CSS */`;
    document.head.appendChild(style);
}
```

**方式二：扩展 css 文件**（如 `无名美化` 的 `lib.init.css(lib.assetURL + "extension/xxx/css", "common")`），把样式独立成文件便于维护。

### 5.3 字体

无名杀内置字体（`font/` 目录，均可用 `font-family` 引用，**内联 SVG 文本也能用**）：

| 字体 | 风格 | 适用 |
| --- | --- | --- |
| `xiaozhuan` | 小篆 | 印章、玉玺、古风标题 |
| `xinwei` | 新魏体 | 武将名、技能名 |
| `yuanli` | 圆体 | 正文 |
| `huangcao` | 狂草 | 身份、台词 |
| `xingkai` | 行楷 | 说明文字 |
| `shousha` | 手杀体 | UI |

### 5.4 SVG 美术资源

- 推荐**手写 SVG**（无版权风险、可精确控制）：圣旨缎面背景、回纹图案、双龙戏珠徽章、传国玉玺（“受命於天 既壽永昌”用 `xiaozhuan` 字体渲染）——本包 `image/sgz_caomao_dialog_bg.svg`、`image/sgz_caomao_double_dragon.svg` 即为此类。
- 内联 `<svg>`（直接写在对话框 HTML 里）可以用游戏字体；作为 `background-image` 的独立 SVG 文件**不能**用游戏字体（字体不加载），纯图形没问题。
- 独立 SVG 作为图层引用时给 CSS 渐变兜底：图片缺失时界面依然可用。

### 5.5 全局劫持（precontent 的经典用法）

在 `precontent` 中替换引擎方法，实现全局效果（本包音频劫持范例）：

```js
precontent: function () {
    const _originPlayAudio = game.playAudio;
    game.playAudio = function () {
        const args = Array.from(arguments);
        const custom = getCustomPath(_status.event && _status.event.player, args[args.length - 1]);
        if (custom) return _originPlayAudio.call(this, custom);
        return _originPlayAudio.apply(this, arguments);
    };
}
```

同理可劫持 `lib.element.player.useCard`、`lib.element.player.respond`（屏蔽系统语音、注入专属语音）。

### 5.6 卡牌特效（曹髦潜龙卡实战）

通过隐藏技能实时给手牌加 CSS 类（`sgz_caomao_qianlong_ui`）：

```js
sgz_caomao_qianlong_ui: {
    charlotte: true, silent: true, forced: true, priority: -10,
    trigger: { player: ["gainAfter", "loseAfter", "enterGame"], global: ["phaseBefore", "gameStart"] },
    init: function (player) { /* 注入 <style>：SVG 湍流滤镜实现火焰撕裂感 */ },
    content: function () {
        player.getCards('h').forEach(card => {
            card.classList.toggle('qianlong-fire-active', card.hasGaintag('sgz_caomao_shaTag'));
        });
    },
},
```

要点：
- `card.classList` 直接操作手牌 DOM；`hasGaintag` 判断卡牌标签。
- 动画用 CSS keyframes；火焰扭曲用 SVG `feTurbulence` + `feDisplacementMap` 滤镜。
- 时刻注意引擎全局 `div { transition: all .5s }`，交互元素要显式写 `transition`。

### 5.7 动效性能与无障碍

- 大量动画加 `prefers-reduced-motion` 降级（本包已加）。
- 布局动画优先 `transform`/`opacity`，避免触发重排。
- 弹出层 z-index 不要超过 `#control`（z-index 5），否则确认按钮被盖住点不到。

---

## 第六章 音频接入

- 目录建议：`audio/武将代码/技能名序号.mp3`。
- 字段引用：
  - 阵亡语音：`dieAudios: ["ext:大梦千秋/audio/sgz_caomao/die.mp3"]`
  - 技能语音：`audio: "ext:大梦千秋/audio/sgz_caomao:16"`（目录下 1~16.mp3 随机）
- 代码播放：`game.playAudio(\`../extension/大梦千秋/audio/sgz_caomao/sgz_fuyuan${[1,2,3].randomGet()}.mp3\`)`——注意相对路径从游戏根目录解析，需带 `../`。
- 用牌语音：劫持 `game.playAudio` / `useCard` / `respond`（见 5.5）。

---

## 第七章 调试、验证与发布

### 7.1 语法校验

```bash
# 将 ES Module 复制为 .mjs 后用 node 校验（node --check 对 .js 按 CommonJS 解析会误报）
copy character\sgz_caomao.js %TEMP%\check.mjs
node --check %TEMP%\check.mjs
```

### 7.2 冒烟测试（无头 DOM mock）

写一个 Node 脚本 mock 掉 `document/ui/lib/get` 的最小实现，直接调用模块内部函数验证：按钮数量、`link` 值、样式变量、动画延迟等（本包开发时即用此法，能抓出 `cssText` 覆盖自定义属性这类逻辑 bug——mock 里改用一个带真实 CSSOM 行为的最小 style 对象）。

### 7.3 真实渲染验证（无头 Chrome）

引擎的最终视觉效果必须用真实浏览器确认：

```powershell
chrome.exe --headless=new --disable-gpu --no-sandbox --user-data-dir=%TEMP%\chrome_test `
  --window-size=800,600 --virtual-time-budget=4000 --dump-dom file:///path/to/test.html
```

做法：按无名杀真实 CSS 加载顺序引入 `layout/default/layout.css`（全局 div 规则！）与 `layout/mobile/layout.css`（默认布局）、主题 css，复刻对话框 DOM，输出 `getComputedStyle` 与 `getBoundingClientRect`，在 1280/800/640 三种视口下断言“5 个选项同排、不换行、整体缩放”。

### 7.4 常见坑速查

| 现象 | 原因 | 对策 |
| --- | --- | --- |
| 自定义 div 全部叠在一起 | 引擎全局 `div { position:absolute }` | 每个元素显式 `position`/`display`（内联最好）|
| 选项换行堆叠 | flex-wrap + 窗口窄 | nowrap + 固定 flex-basis + 面板 scale |
| 自定义属性失效 | `style.cssText` 覆盖 | 先 cssText 后 setProperty |
| 确认/取消盖住主题框 | `#control` z-index 5 > 对话框 4 | 单选自动确定（ui.confirm.close + ui.click.ok）|
| 联机/录像下界面异常 | 自绘 DOM 无法序列化 | `isMine() && !online && !auto && !video` 才自绘 |
| 动画延迟不生效 | `calc(var(--i) * 70ms)` 旧内核不支持 | 内联 `style.animationDelay` |
| 篆字显示为方块 | SVG 文件（background-image）不加载游戏字体 | 用内联 `<svg>` 或 CSS 文字 |

### 7.5 发布

1. 更新 `info.json`（name/author/version）与 `package.version`、`CHANGELOG.md`。
2. 确认 `extension.js` 中 `name` 与文件夹名一致。
3. 压缩为 zip 或直接放 `extension/` 目录，游戏内扩展菜单启用。
4. 修改扩展内部文件后**重启对局/重载扩展**即可生效，无需重装。

---

## 附录一 常用词汇表（摘自《无名杀代码教程》）

> 以下为教程「常用词汇」节的核心条目，完整版见《无名杀代码教程.md》第 4650 行起。

- **trigger** 触发，**phase** 阶段，**player** 玩家，**current** 当前玩家，**target** 目标，**global** 场上所有角色，**event** 事件，**card** 牌，**handCard** 手牌，**equip** 装备牌，**trick** 锦囊牌，**delay** 延时性锦囊
- **h** 手牌，**e** 装备牌，**j** 判定牌，**hp** 体力，**maxHp** 体力上限，**damage** 受伤，**lose** 失去，**recover** 回复体力，**discard** 弃置牌，**draw** 摸牌，**step** 步骤
- **fire** 火焰伤害，**thunder** 雷属性伤害，**storage** 技能标记存储，**mark** 标记，**countCard** 牌数，**result** 结果，**random** 随机，**compare** 拼点，**gain** 获得
- **equip1** 武器 **equip2** 防具 **equip3** 防御马 **equip4** 进攻马 **equip5** 宝物 **equip6** 所有马
- `game.over(bool)` 游戏结束；`game.createCard2` 生成的牌洗牌不消失
- `get.strNumber(num)` 数字转 A/J/Q/K；`get.distance(player, target)` 距离
- `card.hasGaintag(str)` 卡牌是否带文字标记
- `get.position(card, true)` 读取处理区；`player.getLastUsed(X)` 获取倒数第 X+1 张用牌事件

---

## 附录二 大梦千秋曹髦文件清单

```
character/sgz_caomao.js
├── 顶部：dmqcFuyuan* 模块（帝诏·权能主题选择框：样式注入、玉玺 SVG、构建函数）
├── character.sgz_caomao：武将数据
├── skills.sgz_fuyuan（+ subSkill.xiaoguo，五个权能：放逐/潜谋/诰封/绝敕/秉尊）
├── skills.sgz_fuyuan_mark / _gaofeng / _juechi / _bingzun（权能标记）
├── skills.sgz_qingtao（限定技·倾讨）
├── skills.sgz_tengyuan（腾渊形态）
└── skills.sgz_caomao_qianlong_ui（潜龙卡特效 UI）

image/sgz_caomao.jpg / sgz_caomao2.jpg         武将立绘
image/sgz_caomao_dialog_bg.svg                 圣旨缎面背景
image/sgz_caomao_double_dragon.svg             双龙戏珠金徽

audio/sgz_caomao/*.mp3                         技能/阵亡语音
```

---

> 本流程文档随扩展持续更新。开发中如遇引擎新机制（事件系统、UI 体系变化），以 `noname/` 源码与《无名杀代码教程》为准。
